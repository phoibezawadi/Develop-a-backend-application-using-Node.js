


            //question 5
const testArrays = [
    [1, 2, 3, 4, 5],        // Positive integers
    [-1, -2, -3, -4, -5],   // Negative integers
    [0, 0, 0, 0],           // Array of zeros
    [1.1, 2.2, 3.3],        // Decimal numbers
    [],                     // Empty array
];

testArrays.forEach((array, index) = {
    console.log(`Test ${index + 1}: Array [${array}] -> Sum: ${sumOfNumbers(array)}`)
});





        
   